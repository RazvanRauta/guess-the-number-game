<thlogic>
    <attr sel="#headId"
          th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#footerId"
          th:replace="~{fragments/footer :: footerFragment}"/>
    <attr sel="#playId"
          th:href="@{__${T(udemy.springcourse.util.GameMappings).PLAY}__}"/>
    <attr sel="#welcome"
          th:text="#{game.welcome}"/>
    <attr sel="#play"
          th:text="#{game.play}"/>

</thlogic>